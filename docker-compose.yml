services:
  tickets:
    image: ghcr.io/langdar2/tickets:latest
    ports:
      - "5555:5555"
    volumes:
      - ticket-data:/app/data
    environment:
      - DATA_DIR=/app/data
      - TZ=Europe/Berlin
    restart: unless-stopped

volumes:
  ticket-data:
